apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: restapi
  labels:
    part-of: azul
    section: core
    component: frontend
spec:
  revisionHistoryLimit: 1
  {{- with .Values.restapi.persistentVolumeClaimRetentionPolicy }}
  {{- if .enabled }}
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: {{ .whenDeleted | default "Retain" }}
    whenScaled: {{ .whenScaled | default "Retain" }}
  {{- end }}
  {{- end }}
  selector:
    matchLabels:
      app: restapi
  serviceName: "restapi"
  template:
    metadata:
      name: restapi
      labels:
        app: restapi
        part-of: azul
        section: core
        component: frontend
        allow-ingress-restapi: "true"
        allow-egress-dispatcher: "true"
        allow-egress-opensearch: "true"
        allow-egress-smart-string-finder: "true"
        allow-egress-redis: "true"
        {{- if .Values.external.loki.endpoint }}
        allow-egress-loki: "true"
        {{- end }}
        # Egress for OIDC
        allow-egress-https: "true"
      annotations:
        # ensure deployment restarts if config is changed
        checksum/config1: {{ include (print $.Template.BasePath "/core/restapi/config.yaml") . | sha256sum }}
        checksum/config2: {{ include (print $.Template.BasePath "/core/metastore/config.yaml") . | sha256sum }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "harden.deployment" . | nindent 6 }}
      containers:
      - name: main
        image: {{ include "image.custom" (dict "images" .Values.images "name" "restapi-server")}}
        imagePullPolicy: {{ .Values.images.pullPolicy }}
        livenessProbe:
          httpGet:
            path: /
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30

        {{- include "harden.container" . | nindent 8 }}
        {{- if .Values.restapi.resources }}
        resources:
        {{- .Values.restapi.resources | toYaml | nindent 10 }}
        {{- end }}
        envFrom:
          - configMapRef:
              name: metastore
          - configMapRef:
              name: restapi
          {{- if .Values.plugins.retrohunt.enabled }}
          - configMapRef:
              name: retrohunt
          {{- end }}
        env:
          - name: METASTORE_OPENSEARCH_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.metastoreCreds }}
                key: writer
          {{- if .Values.security.oidc.enable_pat }}
          - name: METASTORE_JWT_SIGNING_SECRET
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.metastoreCreds }}
                key: jwt_signing_secret
          - name: METASTORE_OPENSEARCH_AZUL_SECURITY_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.metastoreCreds }}
                key: opensearch_azul_security_password
          {{- end }}
          - name: METASTORE_PURGE_SHA256_FOLDER
            value: "/purge"
          - name: HOST_IP
            valueFrom:
              fieldRef:
                fieldPath: status.hostIP
          {{- if .Values.plugins.retrohunt.enabled }}
          - name: REDIS_USERNAME
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.redis }}
                key: redis-username
          - name: REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.secrets.redis }}
                key: redis-password
          - name: RETROHUNT_ENABLED
            value: "true"
          {{- else }}
          - name: RETROHUNT_ENABLED
            value: "false"
          {{- end }}
        volumeMounts:
          - name: tmp
            mountPath: /tmp/
          - name: logs
            mountPath: "/logs"
          - name: purge-dir
            mountPath: "/purge/"
          {{- if .Values.CACertificateConfigMap }}
          - mountPath: /cafile
            name: ca-cert
          {{- end }}
      {{- if .Values.external.loki.endpoint }}
      - name: alloy
        image: {{ get .Values.images.external "alloy" | required "missing image for alloy" }}
        {{- include "harden.container" . | nindent 8 }}
        resources:
          requests:
            memory: "150Mi"
            cpu: "50m"
          limits:
            memory: "150Mi"
            cpu: "500m"
        args:
          - run
          - /etc/alloy/config.river
          - --storage.path=/data/alloy
          - --disable-reporting
        volumeMounts:
          - name: restapi-alloy
            mountPath: /etc/alloy
          - name: logs
            mountPath: "/logs"
          - name: alloy-data
            mountPath: /data/alloy
      {{- end }}
      {{- if .Values.restapi.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- range .Values.restapi.topologySpreadConstraints }}
        - labelSelector:
            matchLabels:
              app: restapi
          {{- . | toYaml | nindent 10 }}
        {{- end }}
      {{- end }}
      volumes:
      - name: tmp
        emptyDir:
          sizeLimit: 200Mi
      {{- if .Values.CACertificateConfigMap }}
      - name: ca-cert
        configMap:
          name: {{ .Values.CACertificateConfigMap }}
      {{- end }}
      - name: restapi-alloy
        configMap:
          name: restapi-alloy
      - name: alloy-data
        emptyDir:
          sizeLimit: 200Mi
  volumeClaimTemplates:
  - metadata:
      name: logs
      annotations:
        helm.sh/resource-policy: "keep"
        argocd.argoproj.io/sync-options: "Prune=false"
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: {{ .Values.storageClassName | required (printf "missing storage class for restapi") }}
      resources:
        requests:
          storage: {{ .Values.restapi.logPvcSize }}
  - metadata:
      name: purge-dir
      annotations:
        helm.sh/resource-policy: "keep"
        argocd.argoproj.io/sync-options: "Prune=false"
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: {{ .Values.storageClassName | required (printf "missing storage class for restapi") }}
      resources:
        requests:
          storage: {{ .Values.restapi.purgePvcSize }}
---
{{- $raMinReplicas := .Values.restapi.minReplicas | default .Values.coreHPA.minReplicas }}
{{- $raMaxReplicas := .Values.restapi.maxReplicas | default .Values.coreHPA.maxReplicas }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: restapi
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: restapi
  minReplicas: {{ $raMinReplicas }}
  maxReplicas: {{ $raMaxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.restapi.averageUtilization | default .Values.coreHPA.averageUtilization }}
{{- if .Values.restapi.pdb.enabled }}
{{- if (lt (int $raMinReplicas) 2) }}
{{- fail "restapi needs more than one replica when running with a poddisruptionbudget"}}
{{- end }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: restapi-pdb
spec:
  minAvailable: {{ .Values.restapi.pdb.minAvailable }}
  selector:
    matchLabels:
      app: restapi
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: restapi
  labels:
    app: restapi
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8000"
spec:
  selector:
    app: restapi
  ports:
  - port: 8000
    name: http
    protocol: TCP
